#header
  .header
    #logo
      =link_to logo, '/'
  %ul.nav
    //%li= "#{link_to "About", "/about"}&nbsp;&nbsp;|&nbsp;&nbsp;"
    %li= "#{link_to "Finder", FINDER_API_URL}&nbsp;&nbsp;|&nbsp;&nbsp;" 
    %li= "#{link_to "Maker", MAKER_API_URL}&nbsp;&nbsp;"
    - if logged_in?
      - if current_user.admin?
        %li= "|&nbsp;&nbsp;#{link_to "Atlases", user_atlases_path(current_user.login)}"
        %li= "&nbsp;&nbsp;|&nbsp;&nbsp;#{link_to "Admin", blurbs_path }"
        - if @atlas
          %li= "&nbsp;&nbsp;|&nbsp;&nbsp;#{link_to "Edit", edit_atlas_path(@atlas.id)}"
    - else
      / %li= "|&nbsp;&nbsp;#{link_to "Login", login_path}"